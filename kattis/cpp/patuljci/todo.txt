

This can be two simple (nested) for loops:

for (i = 0; i < N; i++) {
    for (j = i+1; j < N; j++) {
        /* Instead of repeatedly summing up 7 numbers, sum up the 9,
        then iteratively check two subtractions.
        */
    }
}

