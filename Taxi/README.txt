
the 'taxi' file in this dir is the EXE built from C++ source checked out
in the ~/src/git/Taxi repo.

The other files are effectivly Taxi source, and the filename can simply
be passed as first CLI arg to the EXE:

    $ ./taxi sixTimesSeven.txt


